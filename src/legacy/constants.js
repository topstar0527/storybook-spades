import keymirror from "key-mirror";

export const STATUS = keymirror({
    AVAILABLE: null,
    BOOKED: null
});

export const ROUTES = keymirror({
    HOME: null,
    FLOORPLAN: null,
    BOOKING: null
});
